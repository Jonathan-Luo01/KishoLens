[project]
name = "data_pipeline"
version = "0.1.0"
description = "Ingestion and Scraper Pipeline for KishoLens"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.24.0",
    "selectolax>=0.3.12",
    "pydantic>=2.0",
    "sqlmodel>=0.0.8",
]
