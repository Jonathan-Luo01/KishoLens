[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for KishoLens"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn>=0.22.0",
    "sqlmodel>=0.0.8",
]
