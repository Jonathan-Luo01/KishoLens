{
  "name": "kisholens-monorepo",
  "version": "0.1.0",
  "private": true,
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": ".venv\\Scripts\\uvicorn kisholens.api.main:app --reload",
    "dev:pipeline": ".venv\\Scripts\\python -m kisholens.pipeline.main",
    "dev:ml": ".venv\\Scripts\\python -m kisholens.ml.main",
    "dev": "concurrently \"npm:dev:frontend\" \"npm:dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
